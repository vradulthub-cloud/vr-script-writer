## Design Context

### Users
Small internal team of 7 (3 admins, 3 editors) managing video production workflows for 4 adult content studios. Used daily, in-depth — not casual browsing. Users are comfortable with dense UIs and need speed over hand-holding. Context is typically a desktop browser in a work setting. Power users who will notice inconsistencies.

### Brand Personality
**Three words:** precise · cinematic · backstage

The tool lives behind the scenes of a production operation. It should feel like the equipment rack in a film studio — purposeful, authoritative, no wasted space. Not glamorous, not grim — just completely in control.

### Aesthetic Direction
- **Theme:** Dark, always. Not "dark mode as a preference" — dark as the primary reality of this interface.
- **Reference feel:** Linear, Raycast, Vercel Dashboard — confident use of dark surfaces, restrained accent use, excellent information density.
- **Anti-references:** NO purple/cyan gradient combos, NO gradient text, NO glowing card borders, NO AI startup aesthetic. NO gray enterprise table grids, NO blue primary buttons, NO sidebar-with-accordion nav.

### Color System

#### Surfaces (OKLCH, tinted toward lime hue 82°)
All neutral surfaces carry a subtle warm tint (chroma 0.006–0.012 at hue 82°) so the UI reads as cohesive rather than dead gray.

| Token | Value | Use |
|-------|-------|-----|
| `--color-base` | `oklch(17% 0.012 82)` | Page background |
| `--color-surface` | `oklch(20% 0.01 82)` | Cards, panels |
| `--color-elevated` | `oklch(24% 0.011 82)` | Popovers, dropdowns |
| `--color-border` | `oklch(29% 0.008 82)` | Visible separators |
| `--color-border-subtle` | `oklch(22% 0.006 82)` | Low-contrast dividers |

#### Text (warm-tinted)
| Token | Value | Use |
|-------|-------|-----|
| `--color-text` | `oklch(92% 0.005 82)` | Primary content |
| `--color-text-muted` | `oklch(70% 0.008 82)` | Secondary, metadata |
| `--color-text-faint` | `oklch(58% 0.006 82)` | Tertiary, placeholders |

#### Primary Action — Lime Green
| Token | Value | Use |
|-------|-------|-----|
| `--color-lime` | `#bed62f` | Write-action buttons |
| `--color-lime-dim` | `#8fa820` | Hover/pressed variant |
| `--color-lime-ink` | `#0d0d0d` | Text on lime backgrounds |
| `--color-cta` | alias → lime | Semantic: Generate, Approve, Submit |
| `--color-nav-active` | alias → lime | Sidebar active-item icon tint |

#### Studio Identity
| Studio | Token | Hex | Context |
|--------|-------|-----|---------|
| FPVR (FuckPassVR) | `--color-fpvr` | `#f97316` | Orange |
| VRH (VRHush) | `--color-vrh` | `#8b5cf6` | Purple |
| VRA (VRAllure) | `--color-vra` | `#ec4899` | Pink |
| NJOI (NaughtyJOI) | `--color-njoi` | `#3b82f6` | Blue |

Studio colors dominate when in that studio's context. They are identity anchors, not decoration.

#### Partner-Platform Palette
Hues sit outside both the studio palette and lime so that platform × studio filtering never collides.

| Platform | Token | Hex |
|----------|-------|-----|
| SLR | `--color-platform-slr` | `#38bdf8` (sky-400) |
| POVR | `--color-platform-povr` | `#f59e0b` (amber-500) |
| VRPorn | `--color-platform-vrporn` | `#34d399` (emerald-400) |

#### Status
| Token | Hex | Use |
|-------|-----|-----|
| `--color-ok` | `#22c55e` | Success, healthy |
| `--color-warn` | `#eab308` | Attention needed |
| `--color-err` | `#ef4444` | Error, critical |

### Typography

#### Font Stack (shipped)
| Role | Font | Weight | Use |
|------|------|--------|-----|
| Display-hero | Anton | 400 | Page-level h1 via `.display-hero` — condensed, film-slate feel |
| Display/headings | General Sans | 500–700 | h1, h2, panel titles, nav labels |
| Body | Geist Sans | 400 | All body text, form inputs |
| Monospace | Geist Mono | 400 | IDs, dates, currency, code |
| Serif/writing | Newsreader | 400–700 | Writing Room output, compliance forms |

#### Type Scale (8 steps)
| Token | Size | Use |
|-------|------|-----|
| `--text-xs` | 11px | Captions, metadata |
| `--text-sm` | 12px | Secondary labels |
| `--text-base` | 14px | Body text |
| `--text-md` | 16px | Emphasized body, large list rows |
| `--text-lg` | 18px | Subheadings |
| `--text-title` | 20px | Content-bearing section titles |
| `--text-xl` | 28px | Section headings |
| `--text-display` | clamp(2.25rem, 1.5rem + 1.6vw, 2.75rem) | Page-level h1 |

Headings use `letter-spacing: -0.02em` (h1 gets `-0.03em`). h3 is uppercase-tracked chrome at 13px/0.06em — a categorical label, not content.

### Paper Surfaces

The app has two paper-like sub-themes embedded within the dark shell:

**Dark paper (Writing Room):** Warm desaturated surface for script/description output. Uses Newsreader serif. Tokens: `--color-paper`, `--color-paper-text`, `--color-paper-sub`, `--color-paper-rule`.

**Light paper (Compliance):** Talent-facing iPad UI for on-set document signing. Cream tones, large touch targets. Tokens: `--color-doc-paper`, `--color-doc-cream`, `--color-doc-ink`, `--color-doc-soft`, `--color-doc-faint`, `--color-doc-rule`.

### Design Principles
1. **Information density first.** This is a pro tool used all day. Prioritize fitting more on screen over breathing room. Tight line-height, compact spacing, minimal chrome.
2. **Studio color owns its context.** When working in FPVR, orange is the dominant accent. When in VRH, purple leads. Lime green recedes to action-only use (submit, save, approve, CTA).
3. **Lime green means "this commits something."** Any button or chip tinted lime must perform a write on click — Save, Apply, Approve, Start, Submit, Find, Undo, Generate. Active-nav fill also uses lime at ≤12% mix because "you are here" is itself navigational state. Non-committing affordances (Edit, Cancel, filter toggles, Refresh, Preview) must be neutral outlined buttons. If you want lime for emphasis, don't — use font weight instead.
4. **Weight and size carry hierarchy — not color.** Use font weight (400→700) and size steps aggressively. Reserve color for status and identity, not emphasis.
5. **Nothing decorates.** Every visual element must communicate something: status, identity, hierarchy, or interactivity. Purely decorative elements (dividers, background patterns, ambient glow) are banned unless they orient the user.
